-- description: Allow a picture of one's own on individual part lots

-- Лот — это партия, купленная одним куском: пакет с барахолки, разобранный
-- набор, коробка с рук. Карточка показывает мозаику из четырёх первых
-- деталей, и партии получаются на одно лицо. Своя фотография — то, по чему
-- партию узнают: та самая коробка, тот самый пакет.
--
-- Хранится имя файла, а не сам файл: картинки лежат рядом с остальными, в
-- папке данных, и попадают в те же резервные копии.
ALTER TABLE "bricktracker_individual_part_lots"
ADD COLUMN "image" TEXT;
